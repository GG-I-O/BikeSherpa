import { StyleSheet } from "react-native";

const formStyle = StyleSheet.create({
    container: {
        height: '100%',
        padding: 8
    },
    elements: {
        alignItems: 'center',
        gap: 8
    },
    input: {
        minWidth: '50%'
    }
});

export default formStyle;